version: 2
updates:
  # Enable version updates for GitHub Actions
  - package-ecosystem: "github-actions"
    # Workflow files stored in the default location of `.github/workflows`
    # Don't specify `/.github/workflows`. Use `directory: "/"`.
    directory: "/"
    schedule:
      interval: "weekly"
