{
    "name": "npm Packages",
    "id": "npm-packages",
    "version": "0.1.0-dev",
    "description": "Install global npm packages.",
    "options": {
        "npmPackages": {
            "type": "string",
            "default": "",
            "description": "Comma-separated list of global npm packages to install."
        },
        "minReleaseAge": {
            "type": "string",
            "default": "7",
            "description": "Minimum npm release age in days. An empty string completely disables this feature."
        }
    }
}
