{
    "name": "Pi",
    "id": "pi",
    "version": "0.1.0-dev",
    "description": "Install the Pi coding agent CLI as a global package.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of the Pi coding agent global package to install."
        },
        "minReleaseAge": {
            "type": "string",
            "default": "7",
            "description": "Minimum npm release age in days. An empty string completely disables this feature."
        }
    }
}
