{
    "name": "Pi",
    "id": "pi",
    "version": "1.0.0",
    "description": "Install the Pi coding agent CLI.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of the Pi package to install."
        },
        "minReleaseAge": {
            "type": "string",
            "default": "7",
            "description": "Minimum release age in days for npm installation."
        }
    }
}
