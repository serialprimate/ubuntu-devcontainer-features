{
    "name": "pipx Packages",
    "id": "pipx-packages",
    "version": "0.1.0-dev",
    "description": "Install pipx packages globally.",
    "options": {
        "pipxPackages": {
            "type": "string",
            "default": "",
            "description": "Comma-separated list of packages to install globally with pipx."
        },
        "minReleaseAge": {
            "type": "string",
            "default": "7",
            "description": "Minimum release age in days for package installation."
        }
    }
}
