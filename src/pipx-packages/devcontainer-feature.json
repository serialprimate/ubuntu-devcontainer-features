{
    "name": "pipx Packages",
    "id": "pipx-packages",
    "version": "0.1.0-dev",
    "description": "Install pipx global packages.",
    "options": {
        "pipxPackages": {
            "type": "string",
            "default": "",
            "description": "Comma-separated list of pipx global packages to install."
        },
        "minReleaseAge": {
            "type": "string",
            "default": "7",
            "description": "Minimum release age in days for package installation."
        }
    }
}
