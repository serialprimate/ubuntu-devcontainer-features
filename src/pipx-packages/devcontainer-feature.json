{
    "name": "pipx Packages",
    "id": "pipx-packages",
    "version": "0.1.0-dev",
    "description": "Install pipx global packages.",
    "options": {
        "pipxPackages": {
            "type": "string",
            "default": "",
            "description": "Space-separated list of pipx global packages to install."
        },
        "pipxCooldown": {
            "type": "string",
            "default": "7",
            "description": "Value passed to pipx's --cooldown option in days. An empty string omits the option."
        }
    }
}
