{
    "name": "APT Packages",
    "id": "apt-packages",
    "version": "0.1.0-dev",
    "description": "Install default apt OS packages.",
    "options": {
        "aptPackages": {
            "type": "string",
            "default": "",
            "description": "Space-separated list of default apt OS packages to install."
        }
    }
}
