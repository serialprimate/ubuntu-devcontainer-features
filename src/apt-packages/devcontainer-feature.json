{
    "name": "APT Packages",
    "id": "apt-packages",
    "version": "0.1.0-dev",
    "description": "Install apt packages.",
    "options": {
        "aptPackages": {
            "type": "string",
            "default": "",
            "description": "Comma-separated list of apt packages to install."
        }
    }
}
