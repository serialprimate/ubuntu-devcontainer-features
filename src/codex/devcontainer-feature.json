{
    "name": "Codex",
    "id": "codex",
    "version": "0.1.0-dev",
    "description": "Install the Codex coding agent CLI as a global package.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of the Codex coding agent global package to install."
        },
        "minReleaseAge": {
            "type": "string",
            "default": "7",
            "description": "Minimum release age in days for npm installation."
        }
    }
}
